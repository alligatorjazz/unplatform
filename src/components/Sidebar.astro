---
import ArchiveButton from "./ArchiveButton.astro";
import Donate from "./Donate.astro";
import PreviousIssues from "./PreviousIssues.astro";
import Prompt from "./Prompt.astro";
import Subscribe from "./Subscribe.astro";
import { SubscribeForm } from "./SubscribeForm";

interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<section
	class={[
		"static md:sticky md:top-1/3 h-full flex flex-col justify-start items-center",
		className,
	].join(" ")}
>
	<!-- <ArchiveButton showPrompt class={"w-64 px-2 py-1"} /> -->
	<SubscribeForm client:only="react" className="w-full text-sm" />
	<Donate />
</section>
