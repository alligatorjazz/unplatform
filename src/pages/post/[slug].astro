---
import { getEntry } from "astro:content";
import BlogPost from "../../layouts/BlogPost.astro";

const { slug } = Astro.params;

const post = slug ? await getEntry("posts", slug) : null;
---

{post && <BlogPost post={post} />}
